// src/dashboard/pages/ChatSupportPage.jsx

import React from 'react';

export default function ChatSupportPage() {
  return (
    <div>
      <h2>Chat Support</h2>
      <p>This is where the moderator can chat with users (if chat logic is implemented).</p>
      {/* Nếu bạn đã có component chat (ví dụ WebSocket), import và render ở đây */}
    </div>
  );
}
